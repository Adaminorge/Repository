{% extends "base.html.twig" %}
{%  block body %}

    <div class="container pl-0">
    <ul class="list-group">
        <li class="list-group-item active"><h4>Graff Liste</h4></li>
    {% for chart in charts %}
        <li class="list-group-item"> <a href="{{ url("show_chart",{"id":chart.id}) }}">{{ chart.id }} {{ chart.title }}</a></li>

    {% endfor %}
    </ul>
    </div>


{% endblock %}